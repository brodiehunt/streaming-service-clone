{
  "extends": ["next/core-web-vitals", "next/typescript", "prettier"],
  "overrides": [
    {
      "files": ["**/*.d.ts"],
      "rules": {
        "no-var": "off" // Disable the rule specifically for .d.ts files
      }
    }
  ]
}
